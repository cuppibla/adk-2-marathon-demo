"""Marathon Race Day Coach — ADK 2.0 workflows.

Modes:
- strategy_graph: Pillar 1 — single runner, graph workflow (parallel fetches + router + agent)
- concierge:      Pillar 2 — chat coordinator + parallel single-turn specialists (coming in Phase 1)
- team_planner:   Pillar 3 — dynamic fan-out over a runtime-sized roster (coming in Phase 2)
"""
